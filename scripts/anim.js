// Logo Animation Script

var logoShape = document.getElementsByClassName('blank');
//find cephalopod-logo shapes with class of 'blank'

window.onload = function() { //after the page loads
	"use strict";
	var logoShape; //select the logo-shape variable
    logoShape.className = 'draw'; //change the value of the class attribute
};